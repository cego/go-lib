---
name: actions
on:
  push:
    tags-ignore:
      - '**'
    branches:
      - main
  pull_request:

jobs:

  golangci-lint:
    steps:
      - name: Golangci-lint
        uses: golangci/golangci-lint-action@v8.0.0